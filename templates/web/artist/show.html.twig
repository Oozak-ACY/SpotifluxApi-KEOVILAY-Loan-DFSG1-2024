{% extends 'base.html.twig' %}

{% block title %}Artist{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <h1>Artist</h1>

            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ artist.id }}</td>
                    </tr>
                    <tr>
                        <th>Name</th>
                        <td>{{ artist.name }}</td>
                    </tr>
                </tbody>
            </table>

            <a href="{{ path('app_web_artist_index') }}" class="btn btn-outline-secondary">back to list</a>

            <a href="{{ path('app_web_artist_edit', {'id': artist.id}) }}" class="btn btn-warning">edit</a>

            {{ include('web/artist/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
